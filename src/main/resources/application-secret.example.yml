app:
  jwt:
    secret: change-me-local-jwt-secret-change-before-use
  internal-auth:
    clients:
      demo-user-server:
        secret: change-me
        previous-secret:
  map:
    analysis:
      provider: fake
  openai:
    api-key:
    base-url: https://api.openai.com/v1
    model: gpt-5.6
